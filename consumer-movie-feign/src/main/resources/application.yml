server:
  port: 8020
spring:
  application:
    name: consumer-movie-feign
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/
      #defaultZone: http://discovery2:8762/eureka/, http://discovery1:8761/eureka/
    instance:
      preferIpAddress: true
ribbon:
  eureka:
    enable: true # 默认值为true，可以不必配置